import {PostModel, PostWithIdModel} from "../models/postsModels";import {blogsCollection, postsCollection} from "../../../mongodb/db";export const postsRepository = {    async create(data: PostModel): Promise<PostWithIdModel | null> {        const blog = await blogsCollection.findOne(            {id: data.blogId},            {projection: {name: 1, _id: 0}},        );        console.log('postsRepository.create find blog result:', blog);        if (!blog) {            return null;        }        const newPost: PostWithIdModel = {            id: new Date().getTime().toString(),            title: data.title,            shortDescription: data.shortDescription,            content: data.content,            blogId: data.blogId,            blogName: blog.name,        };        await postsCollection.insertOne(newPost);        return newPost;    },    async findAll(): Promise<PostWithIdModel[]> {        return postsCollection.find({}).toArray();    },    async findById(id: PostWithIdModel['id']): Promise<PostWithIdModel | null> {        return postsCollection.findOne({id});    },    async update(id: PostWithIdModel['id'], data: PostModel): Promise<boolean> {        const blog = await blogsCollection.findOne(            {id: data.blogId},            {projection: {name: 1, _id: 0}},        );        if (!blog) {            return false;        }        const result = await postsCollection.updateOne({id}, {$set: {...data, blogName: blog.name}});        return result.matchedCount === 1;    },    async delete(id: PostWithIdModel['id']): Promise<boolean> {        const result = await postsCollection.deleteOne({id});        return result.deletedCount === 1;    },};