import {DB} from "../../../db/db";import {PostModel, PostWithIdModel} from "../models/postsModels";export const postsRepository = {    create(newPost: PostWithIdModel): PostWithIdModel {        DB.posts.push(newPost);        return newPost;    },    findAll() {        return DB.posts;    },    findById(id: PostWithIdModel['id']): PostWithIdModel | null {        return DB.posts.find((item: PostWithIdModel) => item.id === id) || null;    },    update(postId: PostWithIdModel['id'], newData: PostModel): boolean {        const index: number = DB.posts.findIndex((item: PostWithIdModel) => item.id === postId);        if (index < 0) {            return false;        }        DB.posts[index] = {            ...DB.posts[index],            ...newData,        };        return true;    },    delete(deletedId: PostWithIdModel['id']): boolean {        const index: number = DB.posts.findIndex((item: PostWithIdModel) => item.id === deletedId);        if (index < 0) {            return false;        }        const deletedPost: PostWithIdModel = DB.posts[index];        DB.posts = DB.posts.filter((item: PostWithIdModel) => item.id !== deletedPost.id);        return true;    },};