import {PostInputDataWithBlogNameModel, PostModel} from "../models/postsModels";import {postsCollection} from "../../../mongodb/db";import type {InsertOneResult, UpdateResult} from "mongodb";import {ObjectId} from "mongodb";export const postsRepository = {    async create(data: PostModel): Promise<string> {        const result: InsertOneResult<PostModel> = await postsCollection.insertOne(data);        return result.insertedId.toString();    },    async update(id: string, data: PostInputDataWithBlogNameModel): Promise<boolean> {        const _id: ObjectId = new ObjectId(id);        const result: UpdateResult<PostModel> = await postsCollection.updateOne({_id}, {$set: data});        return result.matchedCount === 1;    },    async delete(id: string): Promise<boolean> {        const _id: ObjectId = new ObjectId(id);        const result = await postsCollection.deleteOne({_id});        return result.deletedCount === 1;    },};