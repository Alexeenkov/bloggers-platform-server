import {IdPathParamsModel, RequestWithPathParamsModel} from "../../../../shared/models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";import {PostWithIdModel} from "../../models";export const getPostHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response<PostWithIdModel>) => {    const foundPost: PostWithIdModel | undefined = Index.posts.find((item: PostWithIdModel) => item.id === req.params.id);    if (!foundPost) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    res        .status(HTTP_STATUSES.OK)        .json(foundPost);}