import {IdPathParamsModel, RequestWithPathParamsModel} from "../../../../shared/models";import {PostWithIdModel} from "../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";export const deletePostHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response) => {    const index: number = Index.posts.findIndex((item: PostWithIdModel) => item.id === req.params.id);    if (index < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    const deletedPost: PostWithIdModel = Index.posts[index];    Index.posts = Index.posts.filter((item: PostWithIdModel) => item.id !== deletedPost.id);    res.sendStatus(HTTP_STATUSES.NO_CONTENT);};