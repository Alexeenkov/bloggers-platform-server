import {Request, Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {postsRepository} from "../../repository/postsRepository";import {PostWithIdModel} from "../../models/postsModels";export const getPostsListHandler = async (_: Request, res: Response<PostWithIdModel[]>) => {    const allPosts: PostWithIdModel[] = await postsRepository.findAll();    res        .status(HTTP_STATUSES.OK)        .json(allPosts);}