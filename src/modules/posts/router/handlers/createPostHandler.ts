import {Response} from "express";import {    ErrorMessagesModel,    RequestWithBodyModel,} from "../../../../shared/models";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";import {PostModel, PostWithIdModel} from "../../models";import {BlogWithIdModel} from "../../../blogs/models";export const createPostHandler = (req: RequestWithBodyModel<PostModel>, res: Response<PostWithIdModel | ErrorMessagesModel>) => {    const foundBlog = Index.blogs.find((blog: BlogWithIdModel) => blog.id === req.body.blogId);    if (!foundBlog) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    const newPost: PostWithIdModel = {        id: new Date().getTime().toString(),        title: req.body.title,        shortDescription: req.body.shortDescription,        content: req.body.content,        blogId: req.body.blogId,        blogName: foundBlog.name,    };    Index.posts.push(newPost);    res.status(HTTP_STATUSES.CREATED).json(newPost);}