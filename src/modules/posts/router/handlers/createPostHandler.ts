import {Response} from "express";import type {ErrorMessagesModel, RequestWithBodyModel} from "../../../../shared/models";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import type {PostInputDataModel, PostModel} from "../../models/postsModels";import {postsService} from "../../application/postsService";export const createPostHandler = async (req: RequestWithBodyModel<PostInputDataModel>, res: Response<PostModel | ErrorMessagesModel>) => {    const newPost = await postsService.create(req.body);    if (!newPost) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    res.status(HTTP_STATUSES.CREATED).json(newPost);}