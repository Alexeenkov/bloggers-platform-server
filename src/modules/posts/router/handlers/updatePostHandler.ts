import {    IdPathParamsModel,    RequestWithPathParamsAndBodyModel,} from "../../../../shared/models";import {PostModel, PostWithIdModel} from "../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";export const updatePostHandler = (req: RequestWithPathParamsAndBodyModel<IdPathParamsModel, PostModel>, res: Response) => {    const indexUpdatedPost: number = Index.posts.findIndex((item: PostWithIdModel) => item.id === req.params.id);    if (indexUpdatedPost < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    Index.posts[indexUpdatedPost] = {        ...Index.posts[indexUpdatedPost],        ...req.body,    };    res.sendStatus(HTTP_STATUSES.NO_CONTENT);}