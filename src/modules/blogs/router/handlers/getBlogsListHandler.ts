import {Request, Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {blogsRepository} from "../../repository/blogsRepository";import {BlogWithIdModel} from "../../models/blogsModels";export const getBlogsListHandler = async (_: Request, res: Response<BlogWithIdModel[]>) => {    const allBlogs: BlogWithIdModel[] = await blogsRepository.findAll();    res        .status(HTTP_STATUSES.OK)        .json(allBlogs);}