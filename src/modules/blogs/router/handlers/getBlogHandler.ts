import {IdPathParamsModel, RequestWithPathParamsModel} from "../../../../shared/models";import {BlogWithIdModel} from "../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";export const getBlogHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response<BlogWithIdModel>) => {    const foundBlog: BlogWithIdModel | undefined = Index.blogs.find((item: BlogWithIdModel) => item.id === req.params.id);    if (!foundBlog) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    res        .status(HTTP_STATUSES.OK)        .json(foundBlog);}