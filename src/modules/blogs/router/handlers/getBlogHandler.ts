import {IdPathParamsModel, RequestWithPathParamsModel} from "../../../../shared/models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {blogsRepository} from "../../repository/blogsRepository";import {BlogModel} from "../../models/blogsModels";export const getBlogHandler = async (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response<BlogModel>) => {    const foundBlog: BlogModel | null = await blogsRepository.findById(req.params.id);    if (!foundBlog) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    res        .status(HTTP_STATUSES.OK)        .json(foundBlog);}