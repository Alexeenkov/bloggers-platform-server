import {IdPathParamsModel, RequestWithPathParamsModel} from "../../../../shared/models";import {BlogWithIdModel} from "../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";export const deleteBlogHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response) => {    const index: number = Index.blogs.findIndex((item: BlogWithIdModel) => item.id === req.params.id);    if (index < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    const deletedBlog: BlogWithIdModel = Index.blogs[index];    Index.blogs = Index.blogs.filter((item: BlogWithIdModel) => item.id !== deletedBlog.id);    res.sendStatus(HTTP_STATUSES.NO_CONTENT);};