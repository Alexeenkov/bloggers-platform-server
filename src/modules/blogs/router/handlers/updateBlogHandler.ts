import {    IdPathParamsModel,    RequestWithPathParamsAndBodyModel,} from "../../../../shared/models";import {Response} from "express";import {HTTP_STATUSES} from "../../../../shared/constants/httpStatuses";import {Index} from "../../../../db";import {BlogModel, BlogWithIdModel} from "../../models";export const updateBlogHandler = (req: RequestWithPathParamsAndBodyModel<IdPathParamsModel, BlogModel>, res: Response) => {    const indexUpdatedBlog: number = Index.blogs.findIndex((item: BlogWithIdModel) => item.id === req.params.id);    if (indexUpdatedBlog < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    Index.blogs[indexUpdatedBlog] = {        ...Index.blogs[indexUpdatedBlog],        ...req.body,    };    res.sendStatus(HTTP_STATUSES.NO_CONTENT);}