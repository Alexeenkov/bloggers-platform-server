import {DB} from "../../../db/db";import {BlogModel, BlogWithIdModel} from "../models/blogsModels";export const blogsRepository = {    create(newBlog: BlogWithIdModel): BlogWithIdModel {        DB.blogs.push(newBlog);        return newBlog;    },    findAll() {        return DB.blogs;    },    findById(id: BlogWithIdModel['id']): BlogWithIdModel | null {        return DB.blogs.find((item: BlogWithIdModel) => item.id === id) || null;    },    update(blogId: BlogWithIdModel['id'], newData: BlogModel): boolean {        const index: number = DB.blogs.findIndex((item: BlogWithIdModel) => item.id === blogId);        if (index < 0) {            return false;        }        DB.blogs[index] = {            ...DB.blogs[index],            ...newData,        };        return true;    },    delete(deletedId: BlogWithIdModel['id']): boolean {        const index: number = DB.blogs.findIndex((item: BlogWithIdModel) => item.id === deletedId);        if (index < 0) {            return false;        }        const deletedBlog: BlogWithIdModel = DB.blogs[index];        DB.blogs = DB.blogs.filter((item: BlogWithIdModel) => item.id !== deletedBlog.id);        return true;    },};