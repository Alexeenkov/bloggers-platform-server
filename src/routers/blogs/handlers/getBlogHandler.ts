import {BlogWithIdModel, IdPathParamsModel, RequestWithPathParamsModel} from "../../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../constants/httpStatuses";import {DB} from "../../../mocks/db";export const getBlogHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response<BlogWithIdModel>) => {    const foundBlog: BlogWithIdModel | undefined = DB.blogs.find((item: BlogWithIdModel) => item.id === req.params.id);    if (!foundBlog) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    res        .status(HTTP_STATUSES.OK)        .json(foundBlog);}