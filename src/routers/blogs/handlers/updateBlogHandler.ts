import {    BlogModel,    BlogWithIdModel,    IdPathParamsModel,    RequestWithPathParamsAndBodyModel,} from "../../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../constants/httpStatuses";import {DB} from "../../../mocks/db";export const updateBlogHandler = (req: RequestWithPathParamsAndBodyModel<IdPathParamsModel, BlogModel>, res: Response) => {    const indexUpdatedBlog: number = DB.blogs.findIndex((item: BlogWithIdModel) => item.id === req.params.id);    if (indexUpdatedBlog < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    DB.blogs[indexUpdatedBlog] = {        ...DB.blogs[indexUpdatedBlog],        ...req.body,    };    res.sendStatus(HTTP_STATUSES.NO_CONTENT);}