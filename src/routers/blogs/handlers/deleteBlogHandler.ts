import {BlogWithIdModel, IdPathParamsModel, RequestWithPathParamsModel} from "../../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../constants/httpStatuses";import {DB} from "../../../mocks/db";export const deleteBlogHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response) => {    const index: number = DB.blogs.findIndex((item: BlogWithIdModel) => item.id === req.params.id);    if (index < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    const deletedBlog: BlogWithIdModel = DB.blogs[index];    DB.blogs = DB.blogs.filter((item: BlogWithIdModel) => item.id !== deletedBlog.id);    res.sendStatus(HTTP_STATUSES.NO_CONTENT);};