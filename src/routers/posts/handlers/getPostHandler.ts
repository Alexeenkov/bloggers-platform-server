import {PostWithIdModel, IdPathParamsModel, RequestWithPathParamsModel} from "../../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../constants/httpStatuses";import {DB} from "../../../mocks/db";export const getPostHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response<PostWithIdModel>) => {    const foundPost: PostWithIdModel | undefined = DB.posts.find((item: PostWithIdModel) => item.id === req.params.id);    if (!foundPost) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    res        .status(HTTP_STATUSES.OK)        .json(foundPost);}