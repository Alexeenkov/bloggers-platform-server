import {    PostModel,    PostWithIdModel,    IdPathParamsModel,    RequestWithPathParamsAndBodyModel,} from "../../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../constants/httpStatuses";import {DB} from "../../../mocks/db";export const updatePostHandler = (req: RequestWithPathParamsAndBodyModel<IdPathParamsModel, PostModel>, res: Response) => {    const indexUpdatedPost: number = DB.posts.findIndex((item: PostWithIdModel) => item.id === req.params.id);    if (indexUpdatedPost < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    DB.posts[indexUpdatedPost] = {        ...DB.posts[indexUpdatedPost],        ...req.body,    };    res.sendStatus(HTTP_STATUSES.NO_CONTENT);}