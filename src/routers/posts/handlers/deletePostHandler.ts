import {PostWithIdModel, IdPathParamsModel, RequestWithPathParamsModel} from "../../../models";import {Response} from "express";import {HTTP_STATUSES} from "../../../constants/httpStatuses";import {DB} from "../../../mocks/db";export const deletePostHandler = (req: RequestWithPathParamsModel<IdPathParamsModel>, res: Response) => {    const index: number = DB.posts.findIndex((item: PostWithIdModel) => item.id === req.params.id);    if (index < 0) {        res.sendStatus(HTTP_STATUSES.NOT_FOUND);        return;    }    const deletedPost: PostWithIdModel = DB.posts[index];    DB.posts = DB.posts.filter((item: PostWithIdModel) => item.id !== deletedPost.id);    res.sendStatus(HTTP_STATUSES.NO_CONTENT);};