import express, {Express, Request, Response} from 'express';import {HTTP_STATUSES} from "./constants/httpStatuses";import {getTestingRouter, getPostsRouter, getBlogsRouter} from "./routers";import {ROUTERS} from "./constants/routers";export const setupApp = (app: Express) => {    app.use(express.json());    app.get('/', (_: Request, res: Response) => {        res.status(HTTP_STATUSES.OK).send('Bloggers platform server');    });    app.use(ROUTERS.blogs, getBlogsRouter());    app.use(ROUTERS.posts, getPostsRouter());    app.use(ROUTERS.testing, getTestingRouter());    return app;}