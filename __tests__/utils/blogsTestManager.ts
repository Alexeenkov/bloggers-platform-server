import request from "supertest";import app from "../../src";import {ROUTERS} from "../../src/constants/routers";import {HTTP_STATUSES, TypeHttpStatus} from "../../src/constants/httpStatuses";import {BlogModel, BlogWithIdModel} from "../../src/models";export const blogsTestManager = {    getAllBlogs: async () => {        return await request(app)            .get(ROUTERS.blogs)            .expect(HTTP_STATUSES.OK);    },    getBlogById: async (        id: BlogWithIdModel['id'],        expectedHttpStatus: TypeHttpStatus,    ) => {        return await request(app)            .get(`${ROUTERS.blogs}/${id}`)            .expect(expectedHttpStatus);    },    createNewBlog: async (        data: BlogModel,        expectedHttpStatus: TypeHttpStatus,    ) => {        return await request(app)            .post(ROUTERS.blogs)            .send(data)            .expect(expectedHttpStatus);    },    updateBlog: async (        id: BlogWithIdModel['id'],        data: BlogModel,        expectedHttpStatus: TypeHttpStatus,    ) => {        await request(app)            .put(`${ROUTERS.blogs}/${id}`)            .send(data)            .expect(expectedHttpStatus);    },    deleteBlog: async (id: BlogWithIdModel['id']) => {        return await request(app)            .delete(`${ROUTERS.blogs}/${id}`)            .expect(HTTP_STATUSES.NO_CONTENT);    },}